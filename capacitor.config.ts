import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.theninjacare.agent',
  appName: 'theNinjaCare_Agent',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
